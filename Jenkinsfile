pipeline {
    agent any

    stages {
        stage('Run Shell Command') {
            steps {
                // Replace this with any shell command you want
                sh 'echo "Hello from Jenkins!"'
            }
        }
    }
}
